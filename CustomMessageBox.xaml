<Window x:Class="VivaldiUpdater.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Message" Height="250" Width="400"
        AllowsTransparency="True" Background="Transparent"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="tinyStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Semi-transparent overlay background -->
    <Border Background="#80000000" CornerRadius="20">
        <!-- Main content card -->
        <Border MinWidth="380" MaxWidth="600" MinHeight="200" MaxHeight="500"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#EFD6F3" CornerRadius="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Direction="270" ShadowDepth="5" Color="#40000000" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="60" />
                </Grid.RowDefinitions>

                <!-- Title Bar -->
                <Border Grid.Row="0" Background="#F5798B" CornerRadius="15,15,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <TextBlock x:Name="TitleText" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="16" FontWeight="Bold" Foreground="White"
                                   Text="Title" />
                        
                        <Button
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="CloseButton_Click"
                            Content="×"
                            Cursor="Hand"
                            FontSize="18"
                            FontWeight="Bold"
                            Foreground="White"
                            Margin="0,0,5,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FF1493" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15,10">
                    <TextBlock x:Name="MessageText" 
                               TextWrapping="Wrap" 
                               FontSize="14" 
                               Foreground="#333333"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               TextAlignment="Center"/>
                </ScrollViewer>

                <!-- Buttons Area -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button x:Name="OkButton"
                            Width="100" Height="40"
                            Content="OK"
                            Click="OkButton_Click"
                            Style="{StaticResource ApplePriButtonStyle}"
                            Margin="10,0" 
                            Visibility="Visible"/>
                    
                    <Button x:Name="CancelButton"
                            Width="100" Height="40"
                            Content="Cancel"
                            Click="CancelButton_Click"
                            Style="{StaticResource ApplePriButtonStyle}"
                            Margin="10,0"
                            Visibility="Collapsed"/> <!-- Hidden by default -->
                </StackPanel>
            </Grid>
        </Border>
    </Border>
</Window>
